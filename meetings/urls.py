from django.urls import path

from . import views

urlpatterns = [
    # path('create-meeting/', views.CreateMeetingView.as_view()),
    # path('get-all-meetings/', views.GetAllMeetingsView.as_view()),
    # path('get-single-meeting/', views.GetSingleMeetingView.as_view()),
]
